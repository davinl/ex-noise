--- 
id: 9f44b5e0-c38b-012f-c943-12313d2df197
setup:
  commands:
  - bundle install --without production
  - bundle exec rake db:migrate --trace
test:
  commands:
  - bundle exec rake
deploy:
  branch: dev
  commands:
    - heroku run rake db:populate --trace --app enoise-demo
  heroku:
    staging:
      name: ""
    production:
      name: enoise-demo
      branch: dev
      backup: false
      migrate: true
ruby_versions:
- 1.9.2
